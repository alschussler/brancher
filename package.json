{
  "name": "brancher",
  "version": "1.0.0",
  "description": "CLI to create backport PRs for multiple version branches from a single source PR",
  "type": "module",
  "bin": "lib/index.js",
  "scripts": {
    "build": "tsc"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  }
}
